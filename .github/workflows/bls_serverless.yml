name: Build Serverless BLS Signer

on:
  push:
    branches:
      - main
    paths:
      - apps/cookbooks/serverless/ethereum/bls_signatures/**

env:
  GO111MODULE: on

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.19

    - name: Cache Go Modules
      uses: actions/cache@v2
      with:
        path: ~/go/pkg/mod
        key: go-mod-${{ hashFiles('**/go.sum') }}

    - name: Build Go app
      run: |
        mkdir -p apps/external
        cd apps/external
        git clone https://github.com/zeus-fyi/web3.git --branch master --single-branch
        cd ../cookbooks/serverless/ethereum/bls_signatures
        go build -o main
        zip main.zip main

    - name: Upload binary as artifact
      uses: actions/upload-artifact@v2
      with:
        name: main.zip
        path: apps/cookbooks/serverless/ethereum/bls_signatures
